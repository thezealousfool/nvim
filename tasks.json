{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build test - Current File",
      "type": "shell",
      "command": "zig",
      "args": [
        "test",
        "--test-no-exec",
        "-femit-bin=zig-out/bin/test",
        "${file}"
      ]
    }
  ]
}
